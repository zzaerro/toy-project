# Checkout change evidence

The intended express-checkout behavior is fully settled in the current spec and
introduces no unresolved product decision. A deterministic end-to-end test shows
that pressing Pay twice creates two charges. The defect is reproducible and has
not been fixed. No other changed behavior is unverified.
