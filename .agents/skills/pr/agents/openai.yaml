interface:
  display_name: "Create Pull Request"
  short_description: "Publish an explained PR with review evidence"
  default_prompt: "Use $pr to publish this change as a ready-for-review pull request against the repository's base branch."
