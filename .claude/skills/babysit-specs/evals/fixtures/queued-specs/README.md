# Notify

Notification preferences for a small team tool. Run `npm run dev` and open
http://localhost:4173.

Delivery is organised by **channel**. Every channel carries its own mute
switch, set on the preferences screen.
