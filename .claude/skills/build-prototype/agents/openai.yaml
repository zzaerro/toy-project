interface:
  display_name: "Build Prototype"
  short_description: "Prototype a product or compare one decision"
  default_prompt: "Use $build-prototype with effort=standard to build the requested product prototype or compare alternatives for the current user situation, in the project's existing style."
